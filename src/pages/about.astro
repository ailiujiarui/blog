---
import BaseLayout from '../layouts/BaseLayout.astro';
import { SITE } from '../consts';
---

<BaseLayout title="关于" description={`关于 ${SITE.author}`}>
  <section class="mx-auto max-w-3xl px-4">
    <header class="mb-10 animate-fade-up border-b border-border/80 pb-8 text-center">
      <div class="mx-auto mb-6 grid h-24 w-24 place-items-center border border-fg bg-surface text-3xl font-bold shadow-paper">
        {SITE.author.slice(0, 1)}
      </div>
      <h1 class="font-serif text-4xl font-semibold md:text-5xl">
        <span>关于我</span>
      </h1>
      <p class="mt-3 text-muted">{SITE.author} · {SITE.email}</p>
    </header>

    <div
      class="prose glass max-w-none p-8 dark:prose-invert prose-h2:font-bold"
    >
      <p class="text-lg leading-relaxed">
        你好，我是 <strong>{SITE.author}</strong>。这是我的个人博客 ——
        <span class="font-semibold text-accent">{SITE.title}</span>。
      </p>

      <h2>这里写什么</h2>
      <ul>
        <li>前端、后端、工程化的<strong>技术笔记</strong></li>
        <li>设计、颜色、动效的<strong>灵感碎片</strong></li>
        <li>偶尔的<strong>生活随笔</strong></li>
      </ul>

      <h2>这个博客</h2>
      <p>
        由 <a href="https://astro.build" target="_blank" rel="noopener">Astro</a> 强力驱动，
        视觉采用纸面杂志和工程批注混合的风格。源码完全开源，欢迎 star。
      </p>

      <h2>联系方式</h2>
      <p>
        想聊点什么？欢迎通过下方渠道找到我：
      </p>
    </div>

    <div class="mt-8 flex flex-wrap justify-center gap-3">
      {SITE.social.github && (
        <a href={SITE.social.github} target="_blank" rel="noopener" class="glass px-5 py-3 font-medium transition-transform hover:-translate-y-0.5">
          GitHub
        </a>
      )}
      {SITE.social.twitter && (
        <a href={SITE.social.twitter} target="_blank" rel="noopener" class="glass px-5 py-3 font-medium transition-transform hover:-translate-y-0.5">
          Twitter
        </a>
      )}
      {SITE.social.email && (
        <a href={SITE.social.email} class="glass px-5 py-3 font-medium transition-transform hover:-translate-y-0.5">
          Email
        </a>
      )}
    </div>
  </section>
</BaseLayout>
